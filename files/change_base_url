/**
 * Base URL (optional).
 * It is not allowed to have a trailing slash; Drupal will add it
 * for you.
 */

$base_url = 'https://dlhost1.lib.utk.edu';

/**
 * $conf['https'] = TRUE;
 * $cookie_domain = 'dlhost1.lib.utk.edu';
 * $conf['maintenance_theme'] = 'adminimal';
 * $conf['theme_debug'] = TRUE;
 */

/**
 * Prevent Cross-Site Request Forgery (CSRF) clickjacking
 */

$conf['x_frame_options'] = 'SAMEORIGIN';
